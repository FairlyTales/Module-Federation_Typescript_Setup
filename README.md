# Module-Federation Typescript setup

Small example on Typescript integration into the module federation project. The core idea is to set up paths to shared modules both in
Webpack and Typescript configs.
